/*
_handleProgressClick: function(event) {
    const percent = event.clientX * 100 / window.outerWidth;

    this._audio.currentTime = this.state.duration * (percent / 100);
},

_getPlayProgress() {
    return (this.state.currentTime * 100 / this.state.duration) || 0;
},
*/
